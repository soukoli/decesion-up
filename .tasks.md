1. ✅ DONE - Pull-to-refresh funguje na všech 3 worldech, refreshuje vše najednou (fetchData refreshuje všechny API). Nested swiper na Feed stránce má touchStartPreventDefault=false aby neblokoval vertical gesture.

2. ✅ DONE - Globe modal close button opraven: přidán safe-area-inset-top padding pro iPhone notch, zvětšen tap target (p-3), přidán shadow-lg a active:scale-95 feedback.

3. ✅ DONE - Voice recording modul funguje přes Web Speech API (SpeechRecognition). Je integrován v MobilePodcastNoteSheet.tsx a VoiceRecorder.tsx. Podporuje cs-CZ a en-US jazyk. Na iOS Safari není podporován (browser limitation), na Chrome/Android funguje. Kód správně řeší permissions, error states a auto-restart.

4. ✅ DONE - Zprávy: po kliku se nyní otevře NewsDetailDrawer (spodní panel/drawer) místo navigace pryč. Drawer zobrazí přeložený titul+popis (auto-překlad při cs jazyce), datum, zdroj, kategorii, credibility badge. Tlačítko "Číst originál" pro otevření v novém tabu. Zavření přes X nebo tap na backdrop.

5. ✅ DONE - Poznámky přepracovány: nový header "Nápady" s tlačítkem +, možnost vytvořit vlastní poznámku (ne jen z podcastu), API upraveno aby přijalo volné poznámky bez podcast context. List zobrazuje odkud poznámka je, datum, preview textu. Po rozkliku celá poznámka s edit/copy/delete.

Všechny tásky splněny. Design zachován (dark slate-950, amber accents, rounded-xl karty). Žádné nové dependencies přidány. Build prošel bez chyb.